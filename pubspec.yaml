name: dicee
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0" # Updated to support null safety

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2 # Updated for null safety compatibility

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - images/
